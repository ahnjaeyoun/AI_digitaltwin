services:
  broker:
    ports:
      - "${MQTT_BIND_ADDRESS:-127.0.0.1}:${MQTT_HOST_PORT:-1883}:1883"

  timescaledb:
    ports:
      - "${TIMESCALE_BIND_ADDRESS:-127.0.0.1}:${TIMESCALE_HOST_PORT:-6543}:5432"
